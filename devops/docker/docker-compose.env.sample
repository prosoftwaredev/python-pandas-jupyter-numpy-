COMPONENT_NAME=WEB
DJANGO_DEBUG=True

# local postgres installation, available at host postgres
# because it's called 'postgres' in docker-compose file
DATABASE_NAME=postgres
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=
DATABASE_HOST=postgres

#